how to mount server

in file navigator: go to "other locations" -> connect to server (at bottom) -> type: smb://duhsnas-pri.dhe.duke.edu/dusom_radonc_floyd/All_Staff
in command line (for commandline based programs to work):

sudo apt install cifs_utils


sudo mkdir /mnt/floyd_server/
sudo mount -t cifs -o user=jl963,domain=dhe.duke.edu //duhsnas-pri.dhe.duke.edu/dusom_radonc_floyd/All_Staff /mnt/floyd_server/
cd /mnt/floyd_server
